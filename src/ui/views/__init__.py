"""UI view modules (incremental extraction from app.py).

Hub navigation lives on ``App`` in ``app.py``; help and first-run dialogs are in
``ui.help_dialogs`` and ``ui.first_run``. Further splits (generate/import builders)
can move here without changing behavior.
"""
